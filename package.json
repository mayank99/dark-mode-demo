{
  "name": "dark-mode-demo",
  "private": "true",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "postcss-preset-env": "^7.2.3",
    "vite": "^2.7.2"
  },
  "postcss": {
    "plugins": {
      "postcss-preset-env": {
        "features": {
          "nesting-rules": "true"
        }
      }
    }
  }
}
